version : "2"
services :
  nginx:
    image: nginx:1.13.12-alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes: 
      - "/nginx-conf/default.conf:/etc/nginx/conf.d/default.conf"
  